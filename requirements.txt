# Med Assist backend runtime + build dependencies (Python 3.12).
# Used by the Dockerfile that ships to HuggingFace Spaces, and by
# local development. Replaces the previous bloated requirements that
# were pulling Flask / spaCy / matplotlib / dvc / mlflow we don't use.

# --- web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
jinja2>=3.1.0

# --- LLM (chat + vision) ---
google-genai>=0.7.0

# --- retrieval ---
sentence-transformers>=3.0.0    # pulls torch + transformers
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26.0

# --- data acquisition + RCP parsing ---
pandas>=2.2.0
openpyxl>=3.1.0
PyMuPDF>=1.24.0
requests>=2.32.0

# --- persistence (Postgres on Oracle Cloud) ---
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0
pyjwt[crypto]>=2.8.0
